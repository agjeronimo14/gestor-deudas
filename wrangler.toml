name = "gestor-deudas"
compatibility_date = "2025-08-01"

# Cloudflare Pages (static + Functions)
pages_build_output_dir = "./public"

# NOTE:
# - Pages NO soporta `migrations` en wrangler.toml.
# - Aplica migraciones con Wrangler CLI (d1 migrations/apply) o con d1 execute.
